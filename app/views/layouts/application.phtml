<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zarpa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/assets/css/application.css">
</head>
<body class="app-screen">
<nav class="navbar navbar-dark app-topbar">
    <div class="container">
        <a class="navbar-brand fw-bold brand-lockup" href="<?= route('home') ?>">
            <img src="/assets/images/mobilete.svg" alt="Mobilete Zarpa">
            <span>Zarpa</span>
        </a>
        <?php if ($this->current_user) : ?>
        <div class="dropdown desktop-only">
            <a class="nav-link text-white dropdown-toggle" data-bs-toggle="dropdown" href="#">
                <i class="bi bi-person-circle"></i> <?= htmlspecialchars($this->current_user->name) ?>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
                <li><span class="dropdown-item-text text-muted small"><?= htmlspecialchars($this->current_user->email) ?></span></li>
                <?php if ($this->current_user->isAdmin()) : ?>
                <li><a class="dropdown-item" href="<?= route('admin.dashboard') ?>"><i class="bi bi-speedometer2"></i> Admin</a></li>
                <?php endif; ?>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item text-danger" href="<?= route('users.logout') ?>"><i class="bi bi-box-arrow-right"></i> Sair</a></li>
            </ul>
        </div>
        <?php endif; ?>
    </div>
</nav>

<main class="app-main mx-auto">
    <div class="flash-stack">
        <?php foreach (\Lib\FlashMessage::get() as $type => $msg) : ?>
            <div class="alert alert-<?= $type ?> alert-dismissible py-2" role="alert">
                <?= $msg ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
            </div>
        <?php endforeach; ?>
    </div>

    <?php require $view ?>
</main>

<?php if ($this->current_user) : ?>
<nav class="mobile-bottom-nav" aria-label="Navegacao principal">
    <a href="<?= route('home') ?>">
        <i class="bi bi-house-door"></i>
        <span>Inicio</span>
    </a>
    <?php if ($this->current_user->isAdmin()) : ?>
    <a href="<?= route('admin.dashboard') ?>">
        <i class="bi bi-speedometer2"></i>
        <span>Admin</span>
    </a>
    <?php else : ?>
    <a href="<?= route('home') ?>">
        <i class="bi bi-map"></i>
        <span>Rotas</span>
    </a>
    <?php endif; ?>
    <a href="<?= route('users.logout') ?>">
        <i class="bi bi-box-arrow-right"></i>
        <span>Sair</span>
    </a>
</nav>
<?php endif; ?>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
<script src="/assets/js/application.js"></script>
</body>
</html>
