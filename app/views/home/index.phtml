<div class="row justify-content-center">
    <div class="col-md-7">
        <div class="card shadow-sm">
            <div class="card-body p-5 text-center">
                <i class="bi bi-lightning-charge-fill text-warning fs-1"></i>
                <h4 class="mt-3">Olá, <?= htmlspecialchars($this->current_user->name) ?>!</h4>
                <p class="text-muted">Você está autenticado na plataforma Zarpa.</p>
                <hr>
                <div class="row text-start mt-3 g-3">
                    <div class="col-sm-6">
                        <div class="bg-light rounded p-3">
                            <small class="text-muted d-block">Nome</small>
                            <strong><?= htmlspecialchars($this->current_user->name) ?></strong>
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="bg-light rounded p-3">
                            <small class="text-muted d-block">E-mail</small>
                            <strong><?= htmlspecialchars($this->current_user->email) ?></strong>
                        </div>
                    </div>
                    <?php if ($this->current_user->birth_date) : ?>
                    <div class="col-sm-6">
                        <div class="bg-light rounded p-3">
                            <small class="text-muted d-block">Nascimento</small>
                            <strong><?= date('d/m/Y', strtotime($this->current_user->birth_date)) ?></strong>
                        </div>
                    </div>
                    <?php endif; ?>
                </div>
                <a href="<?= route('users.logout') ?>" class="btn btn-outline-danger mt-4">
                    <i class="bi bi-box-arrow-right"></i> Sair
                </a>
            </div>
        </div>
    </div>
</div>
